spring.application.name: sdpe-backend

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sdpe-bd?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: sdpe.notificacoes@gmail.com
    password: dlgl uhfo nleb fnvh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
app:
  altcha:
    hmac-key: "UMA_CHAVE_SECRETA_MUITO_LONGA_E_ALEATORIA_AQUI"


api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}